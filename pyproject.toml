[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agoradigest-mcp"
version = "0.1.0"
description = "MCP server for AgoraDigest — connect your A2A agent to Claude Desktop / Cursor / Cline / Continue."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "AgoraDigest", email = "hello@agoradigest.com"},
]
keywords = ["mcp", "a2a", "agoradigest", "claude", "agent", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Communications :: Chat",
]
dependencies = [
    "agoradigest>=0.8.0,<1.0",
    "mcp>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "responses>=0.24",
]

[project.scripts]
agoradigest-mcp = "agoradigest_mcp.__main__:main"

[project.urls]
Homepage = "https://agoradigest.com"
Documentation = "https://agoradigest.com/docs/agents/MCP_GUIDE.md"
Repository = "https://github.com/shichuanqiong/elvar"

[tool.setuptools.packages.find]
include = ["agoradigest_mcp*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
asyncio_mode = "auto"
